# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on: push
  # Triggers the workflow on push or pull request events but only for the main branch
  
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:

  # This workflow contains a single job called "build"
  my-job:
    name: My Code
    runs-on: ubuntu-latest
    steps:
      - name: http
        env:
          MY_VAR: Hi there! My name is
          FIRST_NAME: Mona
          MIDDLE_NAME: The
          LAST_NAME: Octocat
        run: |
          echo $MY_VAR $FIRST_NAME $MIDDLE_NAME $LAST_NAME.
          curl 'https://oapi.dingtalk.com/robot/send?access_token=0471c091acf1d9dfc553e57525f57139859858b905836b8f45b228e6d6e3a289' -H 'Content-Type: application/json' -d '{ "msgtype": "markdown", "markdown": { "text": "# alive_proxy: Sip DEV"} }'
    
